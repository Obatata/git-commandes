# l'état du projet et le tracking des fichiers (verification des modif sur les fichiers du projet)
git status

# Pour tracer le fihcier, il faut le passe en staging :
git add 

# commit les changements pour les passer en git local
git commit -m "add README.md file"
  -m : message 
  
# pour afficher le code Hash SHA1
git show HEAD

# ADD and commit after modying file "README.md" for instance
git commit -a -m "Add e-mail in README.md"

 
# Remote add origin exampe "https://github.com/Obatata/AzureML_SDK_workspaceSetting.git"
git remote add origin https://github.com/Obatata/AzureML_SDK_workspaceSetting.git

# Change the URI (URL) for a remote Git repository?
# For instance we want to change our remour to "git@github.com:Obatata/AzureMl_Run_pipeline.git" (ssh)
git remote set-url origin git@github.com:Obatata/AzureMl_Run_pipeline.git

# Push from "main" to "origin"
git push -u origin main
